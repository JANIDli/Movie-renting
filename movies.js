// Handle movie rentals
function rentMovie(movieId) {
    // Update the availability of the movie in movies.xml
    // Update the user's rental history in users.xml
    alert('Movie rented successfully!');
}

// Handle movie returns
function returnMovie(movieId) {
    // Update the movie's availability and rental status in XML
    alert('Movie returned successfully!');
}
